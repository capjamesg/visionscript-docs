---
title: VisionScript Documentation
layout: default.njk
---

// add breadrcrumbs json ld
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1",
      "name": "Made with VisionScript",
      "item": "https://visionscript.org/gallery/"
    },
    {
      "@type": "ListItem",
      "position": 2",
      "name": "{{ title }}",
      "item": "{{ url }}"
    }
  ]
}
</script>

<div class="breadcrumbs">
    <a href="https://visionscript.org/gallery/">Made with VisionScript</a> &gt; {{ title }}
</div>

<h1 class="p-name">{{ title }}</h1>
<p class="p-summary">{{ description }}</p>
<img src="{{ image }}" alt="{{ title }}" class="u-photo">
<div class="e-content">
{{ content | safe }}
</div>

<h2>See More Projects</h2>
<ul>
    {% for page in collections.gallery %}
        {% if page.data.title != title %}
            <li><a href="{{ page.url }}">{{ page.data.title }}</a></li>
        {% endif %}
    {% endfor %}
</ul>